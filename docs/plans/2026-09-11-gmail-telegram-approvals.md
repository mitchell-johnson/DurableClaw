# Gmail verification and Telegram approvals

1. Exercise the connected Gmail account through an isolated agent conversation. Use a content-free search and, where available, a recipient-free synthetic draft round trip. Never send a test email or change existing mail. Fix demonstrated connector failures with regression coverage.
2. Extend the trusted messaging plugin interface with explicit approval events and complete action cards. Store single-use, expiring callback capabilities in D1, bound to the exact owner, link, conversation, confirmation, and delivered provider message. Keep the authoritative action preview and argument-hash confirmation in the owning Durable Object. Reauthorize at each boundary, then resume only a successfully approved action. Ordinary messages cannot approve. `/approvals` retrieves pending requests; web remains available for previews too large for Telegram.
3. Run focused security and runtime tests, an independent Make-Check completion review, required repository checks, then push main and deploy. Add callback updates to the existing Telegram webhook without dropping updates. Verify Gmail, the deployed app, and a harmless Telegram approval end to end where possible.

No containers or additional execution services are introduced. Provider errors and credentials must not appear in logs. Ambiguous sends and executions are not automatically replayed.

Implementation verification: Gmail search and a recipient-free synthetic draft create/read/delete round trip succeeded in production; no email was sent or existing mail changed. Telegram approvals passed the real D1/Durable Object integration flow and security regressions for exact arguments, owner/link/message binding, expiry, replay, and revocation. Production preview tests cover file writes and Gmail text-file inputs, with web fallback for incomplete or oversized reviews. All 1,327 automated tests, type checks, formatting, vendor integrity, and both Worker bundles passed before release.
